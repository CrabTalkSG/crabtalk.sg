{
  "scripts": {
    "build": "rm -rf dist && mkdir -p dist && cp index.html robots.txt sitemap.xml dist/ && if [ -d public ]; then cp -R public/* dist/; fi"
  },
  "dependencies": {},
  "devDependencies": {}
}