{
  "name": "crabtalk-seo-pages",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "rm -rf dist && mkdir -p dist && cp *.html style.css robots.txt sitemap.xml dist/ && if [ -d public ]; then cp -R public dist/public && cp -R public/* dist/; fi",
    "start": "npx serve dist"
  },
  "devDependencies": {
    "serve": "^14.2.4"
  }
}