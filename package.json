{
  "name": "crabtalk-professional-seo-8-pages",
  "version": "1.0.0",
  "private": true,
  "description": "Professional Crab Talk static website with SEO subpages.",
  "scripts": {
    "build": "rm -rf dist && mkdir -p dist && cp -R . dist && rm -rf dist/dist dist/.git dist/node_modules"
  },
  "devDependencies": {}
}